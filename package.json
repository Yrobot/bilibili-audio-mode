{
  "name": "bilibili-audio-mode",
  "version": "1.0.1",
  "description": "Bilibili Audio Only Mode",
  "homepage": "https://github.com/Yrobot/bilibili-audio-mode",
  "type": "module",
  "scripts": {
    "dev": "tsx build.ts && echo 'Watching...' && esbuild src/main.ts --bundle --outfile=dist/bilibili-audio-mode.user.js --watch",
    "build": "tsx build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
